<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.altervista.ultimaprovaprimadi.ciromelody.kotlin_basic_starter">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:label="Cap.10 ConstraintLayout"
            android:name=".ConstraintLayout"></activity>
        <activity
            android:name=".RecyclerViewAlternateSerious"
            android:label="RecyclerView alternate, sfida a capitolo 9 " />
        <activity
            android:name=".RecyclerViewAlternate"
            android:label="RecyclerView con due item di classi diverse" />
        <activity
            android:name=".RecyclerView"
            android:label="Capitolo 9 RecyclerView" />
        <activity
            android:name=".CriminalIntent"
            android:label="capitolo 8 i Fragment" />
        <activity android:name=".FragmentActivity" />
        <activity android:name=".ModelViewActivity" />
        <activity android:name=".ButtonActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AndroidBasicsStarter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>